
# memcached:    ## already running
# redis:        ## already running
# postgres:     ## already running


# web:          bin/server
rserve:         bin/rserve
web:            bin/rails server
worker:         bin/worker


log:            tail -f -n 100 log/development.log
# guard:        bundle exec guard ## Do manually
